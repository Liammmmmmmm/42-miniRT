#version 430

layout(local_size_x = 1) in;

layout(std430, binding = 0) buffer Output {
    vec3 data[];
};

uniform uint width_render;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    uint idy = gl_GlobalInvocationID.y;
    data[idy * width_render + idx] = vec3(42.0, 0.0, 0.0);
}
